后端-守护线程
supervisorctl reread
supervisorctl update
supervisorctl restart stock-backend
supervisorctl status

后端-手动部署
cd /home/way/stock-v2-min/backend
source venv/bin/activate
uvicorn main:app --host 0.0.0.0 --port 8000

查看报错日志
tail -n 50 /var/log/stock-strategy/backend.err.log

实例
cd /home/way/stock-v2-min
git pull
source backend/venv/bin/activate
pip install -r backend/requirements.txt

supervisorctl restart stock-backend
supervisorctl status
tail -n 50 /var/log/stock-strategy/backend.err.log

sudo systemctl reload nginx
curl http://127.0.0.1:8000/api/strategies/1/history?page=1&page_size=20
sudo systemctl restart nginx
前端
cd /home/way/stock-v2-min/frontend
git pull
npm install
npm run build



# 前端
cd /home/way/stock-v2-min/frontend
git pull
npm install
npm run build


cd /home/way/stock-v2-min/frontend
npm run build
sudo rm -rf /var/www/stock-strategy/*
sudo cp -r dist/* /var/www/stock-strategy/
sudo systemctl reload nginx

# Nginx
nginx -t
systemctl reload nginx

# 后端（只有接口变了才做）
supervisorctl restart stock-backend